{
  "name": "studybuddy-verify",
  "private": true,
  "version": "1.0.0",
  "description": "StudyBuddy prototype verification harness (no runtime deps — in-memory API stub)",
  "scripts": {
    "test": "node plan.test.js"
  }
}
